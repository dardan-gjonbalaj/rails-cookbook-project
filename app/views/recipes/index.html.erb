<h2>Search by ingredient</h2>
  <%= form_tag(recipes_path, method: :get) do %>
  <%= text_field_tag(:search, params[:search]) %>
  <%= submit_tag("Search") %>
  <%end%>
<% @recipes.each do |recipe| %>
  <div>
  <h2><%= link_to recipe.name, recipe_path(recipe)%>
  </div>
  <%end%>