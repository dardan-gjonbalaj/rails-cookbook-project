<h1><%= @recipe.name %> by <%=link_to @recipe.user.name, user_path(@recipe.user.id)%>
  <%= button_to "Edit", edit_recipe_path(@recipe), :method => :get if logged_in? && current_user.id == @recipe.user_id  %>
</h1> 
   
<h3><%= @recipe.time_to_cook %> mins </h4>
<p><%= @recipe.instructions %></p>
<p><% @recipe.recipe_ingredients.each do |ingredient|%></p>
  <div>
    <h4><%= ingredient.quantity%>: <%= ingredient.ingredient.name %></h4>
  </div>
  <%end%>
<%= render 'comments/show_comments', comments: @recipe.comments if @recipe.comments %>

<br />

Add your comment below! (You must be signed in)
<%= render 'comments/form', comment: @recipe.comments.new, recipe: @recipe%>
</div>